form(action="/contactSave/"+record+"/"+grade+"/"+mode method="post")
    section(name="name" required)
        header Contact Information
        p Your name
        input(type="string")
    section(name="mobile")
        header Contact Information
        p Your contact mobile number (optional)
        input(type="string")
    section(name="email" required)
        header Contact Information
        p Your email address
        input(type="email")